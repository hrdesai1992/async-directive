import { AsyncLoadingDirective } from './async-loading.directive';

describe('AsyncLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
